# Moment Weaver · 产品需求文档（PRD）

> 文档版本：v1.1（单人 + Vibe Coding 瘦版）
> 适用范围：Web 端瘦版 MVP（小程序端在二期适配）
> 状态：待评审

---

## 0. 文档说明

本文档是 Moment Weaver（暂定中文名：**时光编织者**）Web 端 MVP 的产品需求基线，用于指导设计、研发、测试与合规评审。文档遵循以下原则：

- **场景通用**：核心能力同时服务「家族口述史 / 个人时光胶囊 / 团队企业故事」三类用户群。
- **合规优先**：以中国《个人信息保护法》《生成式人工智能服务管理暂行办法》《互联网信息服务深度合成管理规定》以及欧盟 GDPR 精神为底线。
- **AI 透明**：任何由模型生成或显著改写的内容必须明确标识。
- **数据最小化**：仅采集业务必需字段，提供遗忘权与可携带权。
- **Web 优先**：本期不实现小程序原生端，但通过接口与前端工程化预留扩展点。

---

## 1. 产品概述

### 1.1 一句话定义
Moment Weaver 是一款 **AI 引导式故事采集与生成平台**：用户通过文字或语音与 AI 采访官对话，上传照片/音视频/文字素材，由 AI 协助梳理时间线、撰写人物小传/家族小传/团队传记，并支持审阅、编辑与可控分享。

### 1.2 价值主张
| 维度 | 用户痛点 | Moment Weaver 的解法 |
| --- | --- | --- |
| 采集 | 面对长辈/同事不知问什么、记不下 | AI 采访官按人物画像动态追问 |
| 整理 | 素材散落、缺乏时间线 | 自动时间线 + 主题聚类 |
| 成稿 | 写不出传记/回忆录 | AI 起草 + 人工润色，AI 内容明确标识 |
| 合规 | 涉及隐私、肖像、跨境 | 显式授权、数据最小化、遗忘权、导出/删除 |
| 分享 | 想给家人/团队看但怕失控 | 受控链接 + 可见范围 + 有效期 |

### 1.3 三类目标场景
1. **家族口述史**：用户 A 采访家中长辈，形成可传承的家族小传与人物志。
2. **个人时光胶囊**：用户 B 记录自己人生阶段，AI 协助生成年度回顾、人生章节。
3. **团队/企业故事**：HR / Brand 团队采访创始人/老员工，沉淀企业史、人物专访。

三类场景共享同一套核心对象（项目 → 人物 → 素材 → 章节 → 成稿），通过「项目类型」与「权限模板」差异化配置。

### 1.4 MVP 范围与边界
> 鉴于本期为单人 + AI 辅助开发，MVP 主动瘦身为"端到端可跑通的最瘦切片"。

**包含（V1 必须上线）**：
- 账号与工作区（手机号 / 邮箱）
- 项目类型：**family** + **personal** 两种（team 留二期）
- 人物档案 + 完整授权流程（合规重头戏）
- AI 采访（**仅文本**；语音/ASR 接口预留）
- 素材库（**图片优先**，音视频/文本片段占位）
- AI 成稿：**人物小传** + **家族小传** 2 模板（年度回顾、专访稿留二期）
- 段落级 AI 标识
- 受控分享（链接 + 密码 + 有效期）
- 合规中心：授权记录、数据导出、账号与项目删除
- Vue3 + Element Plus 桌面端 + Vant 移动端 H5（断点切换）

**不包含（一期明确不做）**：
- 团队/企业项目类型与多角色协同
- 视频采访、实时多人协作
- 移动端原生应用与小程序
- 图像/视频生成、RAG、知识图谱（保留接口，二期）
- Web Push、PWA 高级优化（仅做基础 manifest）
- 国际化（i18n 框架预埋，但只交付 zh-CN）
- 邮件通知（仅站内通知）
- 多模型自动降级（主选 1 个 LLM，备选 1 个手工切换）
- 微服务拆分、K8s、GPU 节点（本期模块化单体 + Docker Compose）

---

## 2. 用户与角色

### 2.1 角色矩阵
| 角色 | 定义 | 关键权限 |
| --- | --- | --- |
| 平台管理员 | 内部运营/合规岗 | 用户管理、模型与提示词管理、内容安全审核、审计日志 |
| 项目所有者（Owner） | 创建项目的个人/组织账号 | 项目配置、邀请协作者、决定成稿发布与删除 |
| 采访者（Interviewer） | 由 Owner 邀请 | 进行采访、补充素材、起草章节 |
| 审稿人（Reviewer） | 由 Owner 邀请 | 阅读、批注、建议修改，不可直接发布 |
| 被采访者（Subject） | 故事主角（可注册可不注册） | 在被采访的人物上确认授权、查看与自己相关的内容、随时撤回 |
| 读者（Reader） | 通过分享链接访问 | 仅能查看被授权范围内的成稿，不可下载原素材 |

### 2.2 关键用户旅程

#### 旅程 A：家族口述史
1. 用户注册并创建「家族小传」项目
2. 添加长辈为「被采访者」人物，发起授权请求（短信/微信/邮件）
3. 与 AI 采访官进行对话，上传老照片
4. AI 整理时间线并生成「爷爷的人物小传」初稿
5. 用户审阅、修改，确认发布
6. 生成家族小传合集，通过微信分享给家人（带有效期与密码）

#### 旅程 B：个人时光胶囊
1. 用户创建「我的年度回顾」项目
2. 选择年份，按提示回答 20 个问题
3. 上传当年照片与日记片段
4. AI 生成年度回顾初稿并标注 AI 生成段落
5. 用户编辑、加入手写回忆，发布为私密胶囊

#### 旅程 C：团队企业故事
1. 企业账号创建「老员工专访」项目，配置公司 VI 与术语表
2. 邀请 5 位采访者、3 位审稿人
3. 采访者分别采访 8 位老员工
4. AI 按统一模板生成 8 篇人物专访 + 1 篇企业史
5. 内部审稿通过后，生成对外可分享的品牌故事页

---

## 3. 信息架构与核心对象

### 3.1 对象关系
```
Workspace（工作区）
└── Project（项目，类型：family | personal | team）
    ├── Member（项目成员，含角色）
    ├── Subject（被采访人物，可关联账号）
    │   └── Authorization（授权记录，状态机）
    ├── InterviewSession（采访会话）
    │   ├── Message（消息对：用户 / AI / 嘉宾）
    │   └── Attachment（附件）
    ├── Asset（素材：image/audio/text）
    ├── TimelineEvent（时间线事件）
    ├── Draft（成稿，模板：人物小传 / 家族小传 / 年度回顾 / 专访稿）
    │   ├── Section（章节，AI 标识粒度）
    │   └── Comment（批注）
    ├── ShareLink（分享链接）
    └── AuditLog（审计日志）
```

### 3.2 项目类型差异化
| 字段 | family | personal | team |
| --- | --- | --- | --- |
| 默认被采访人数 | ≥1 | 0（自己） | ≥1 |
| 推荐模板 | 家族小传、人物小传 | 年度回顾、人生章节 | 专访稿、企业史 |
| 分享默认权限 | 仅邀请 | 私密为主 | 内部可邀请 + 对外链接 |
| 是否启用组织 VI | 否 | 否 | 是（Logo/术语表） |

---

## 4. 功能需求

> 每个功能用「用户故事 + 验收标准 + 边界与异常」三段式描述。

### 4.1 账号与工作区
- **F-AUTH-01 注册/登录**
  - 故事：用户可通过手机号 + 验证码或邮箱 + 密码注册；支持微信扫码（Web 端用开放平台扫码登录）。
  - 验收：注册成功后默认创建个人工作区；密码至少 8 位且含字母+数字。
  - 异常：手机号被注册时提示并支持找回；验证码 60s 内不可重发。

- **F-AUTH-02 多工作区切换**
  - 故事：用户可创建/加入多个工作区，切换时刷新项目列表。
  - 验收：工作区切换 < 1s；支持修改工作区名称。

### 4.2 项目管理
- **F-PROJ-01 创建项目**
  - 故事：选择项目类型 → 填写项目名 → 可选添加被采访者。
  - 验收：项目类型确定模板与默认权限；创建后跳转至项目 Dashboard。

- **F-PROJ-02 邀请协作者**
  - 故事：Owner 通过邮箱/手机号邀请，可指定角色与到期日。
  - 验收：邀请发出后对方收到通知；7 天内未接受自动失效。

### 4.3 被采访者与授权（合规重点）
- **F-SUBJ-01 添加被采访人物**
  - 故事：填写姓名、关系、与本人关系，**不强制要求真实身份信息**。
  - 验收：姓名为必填；身份证号等敏感字段不在表单中提供。

- **F-SUBJ-02 授权流程（核心合规）**
  - 故事：被采访者可注册后确认授权，也可不注册仅通过一次性链接确认。
  - 验收：
    - 授权链接包含项目 ID、采集范围、有效期、撤回方式，必须勾选「我已阅读并同意《被采访者授权书》」才生效。
    - 授权记录保存：授权时间、IP、设备指纹、授权书版本、撤回链接。
    - 被采访者随时可在「我的授权」页面查看并撤回。
  - 异常：未取得授权前不得存储被采访者的语音/视频/生物特征。

- **F-SUBJ-03 撤回授权**
  - 故事：被采访者点击撤回后，关联的原始素材立即不可访问，30 天后从生产库物理删除（中间为「待删除」状态，期间仅数据治理可访问）。
  - 验收：撤回后 Owner 收到通知；项目已发布的成稿中如包含被撤回者内容，须脱敏或下线。

### 4.4 AI 采访
- **F-INT-01 多轮对话**
  - 故事：采访者与 AI 采访官对话，AI 主动追问、记录事实。
  - 验收：上下文长度 ≥ 20 轮；AI 提问基于已采集事实避免重复；提供「换个问法」「深入此话题」快捷指令。

- **F-INT-02 实时事实抽取**
  - 故事：对话过程中实时提取「时间 / 地点 / 人物 / 事件」到时间线候选区。
  - 验收：每条候选可一键确认/编辑/拒绝；确认后写入 TimelineEvent。

- **F-INT-03 采访总结**
  - 故事：会话结束生成「本次采访摘要 + 待补充问题清单」。
  - 验收：摘要包含关键事实、未澄清疑问、建议追问方向。

### 4.5 素材库
- **F-ASSET-01 上传**
  - 支持图片（jpg/png/webp，≤20MB）、音频（mp3/m4a/wav，≤200MB，时长 ≤4h）、纯文本片段。
  - 验收：上传时自动提取 EXIF 时间（图片）、音频时长；敏感信息检测（身份证号、人脸）触发确认弹窗。

- **F-ASSET-02 组织**
  - 故事：按时间线/人物/标签组织。
  - 验收：支持拖拽排序、按时间/上传时间排序、按人物筛选。

- **F-ASSET-03 检索**
  - 故事：按关键词、人物、时间段检索；音频支持 ASR 转写后检索。
  - 验收：转写完成后方可检索；用户可关闭转写以避免文本留存。

### 4.6 时间线
- **F-TL-01 自动生成**
  - 故事：从采访与素材中自动聚合事件，按时间排序。
  - 验收：每条事件可点击查看来源（消息/素材 ID）；置信度低的标记为「待确认」。

- **F-TL-02 手动编辑**
  - 故事：增删改事件，合并相邻事件。

### 4.7 AI 成稿
- **F-DRAFT-01 模板选择**
  - 提供 4 个 MVP 模板：人物小传、家族小传、年度回顾、专访稿。
  - 验收：每个模板有固定章节结构与字数区间（如家族小传 3000-8000 字）。

- **F-DRAFT-02 生成**
  - 故事：选定模板后基于采访事实 + 时间线 + 素材生成。
  - 验收：
    - 生成结果分章节返回。
    - **每段标记「AI 生成」「AI 改写」「人工撰写」三种状态之一**。
    - 引用的素材/事实可点击溯源。

- **F-DRAFT-03 改写与建议**
  - 提供「更正式 / 更口语 / 更简练 / 加入细节」改写按钮。
  - 验收：改写后变更部分仍保持 AI 标识，原始版本可对比查看。

- **F-DRAFT-04 协同审阅**
  - 故事：审稿人可对章节添加批注，Owner 可一键「采纳」或「忽略」。
  - 验收：批注不进入最终成稿；采纳的修改会刷新 AI 标识。

### 4.8 分享与发布
- **F-SHARE-01 受控链接**
  - 故事：生成链接可设置：访问范围（公开/指定邮箱/需登录）、密码、有效期、是否允许复制、是否允许下载 PDF。
  - 验收：默认全部开启限制；过期后链接返回 410。

- **F-SHARE-02 阅读页**
  - 干净的阅读视图，**顶部明确显示「本页内容由 AI 协助生成，已标注 AI 段落」横幅**。
  - 验证：横幅不可关闭（除非项目类型为 personal 且 Owner 选择「隐藏 AI 标识」，但记录此选择用于审计）。

### 4.9 合规中心
- **F-COMP-01 授权中心**
  - 用户与被采访者可在此查看所有有效授权、撤回授权。

- **F-COMP-02 数据导出（可携带权）**
  - 故事：用户可一键导出「我的」原始素材与成稿（zip 包，含 README 说明来源）。
  - 验收：导出请求需二次验证；处理完成后 7 天有效下载链接发送至注册邮箱。

- **F-COMP-03 账号与项目删除（遗忘权）**
  - 故事：用户可申请删除账号或单项目。
  - 验收：
    - 软删除 30 天可恢复（恢复后所有数据完整）。
    - 30 天后物理删除，包括备份（合规例外除外）。
    - 删除范围包括用户创建的所有素材、采访记录、成稿，但**已通过分享链接分发出去的内容不在我们可控范围内，需在删除确认页显著提示**。

- **F-COMP-04 AI 内容标识**
  - 全局策略：所有 AI 生成段落必须在编辑器和发布页可见地标注。
  - 验收：编辑器中通过底色区分；阅读页通过标签和页面顶部横幅双重提示。

- **F-COMP-05 未成年人保护**
  - 故事：识别涉及 14 周岁以下未成年人的内容时，强制要求监护人授权。
  - 验收：未取得监护人授权前，相关素材不可用于成稿，仅可被项目成员私密查看。

### 4.10 通知
- 站内通知中心 + 邮件（默认开启），可关闭营销类。
- 触发点：邀请、授权确认/撤回、AI 成稿完成、分享链接访问异常（>10 次/小时）。

### 4.11 设置
- 个人信息、密码、登录设备管理
- AI 模型偏好（默认/严谨/创意）
- 数据与隐私（直接跳转合规中心）
- 注销账号

---

## 5. 非功能需求

### 5.1 性能
- 首屏 LCP < 2.5s（4G 模拟）
- AI 采访首响 < 1.5s（首 token），平均吐字 ≥ 20 tps
- 素材上传 100MB 以内成功率 ≥ 99.5%

### 5.2 可用性
- 核心服务可用性 99.5%
- 模型降级：AI 服务不可用时，仍可进行采访与素材整理，仅成稿功能禁用并提示

### 5.3 安全
- 全站 HTTPS；HSTS
- 密码 bcrypt 存储；敏感字段（如手机号）加密存储
- 对象存储采用服务端签名直传 + 防盗链
- 越权防护：所有 API 校验资源归属；分享链接使用独立 token，不暴露资源 ID

### 5.4 隐私与合规
- 默认数据最小化：注册只需手机号/邮箱，不强制实名
- 日志脱敏：不打印手机号、身份证、token
- 数据驻留：MVP 仅部署国内区域
- 第三方模型调用：使用境内合规通道（DeepSeek / 通义千问 / GPT-4o 走合规接入），合同约定不用于训练

### 5.5 可访问性
- WCAG 2.1 AA 基础：颜色对比、键盘可达、屏幕阅读器标签
- 字号、对比度可调

### 5.6 国际化
- MVP 仅简体中文；前端 i18n 框架预埋，文案集中在 `locales/zh-CN.json`

### 5.7 端与适配
- **桌面端**（≥1024px）：Element Plus 优先
- **移动端 H5**（<1024px 或窄屏）：Vant UI 自适应
- 关键能力（采访、查看素材、阅读成稿）需在两端跑通
- 小程序端在二期单独立项，本期不实现

---

## 6. AI 使用规范

### 6.1 能力边界
- 允许：基于用户提供的素材进行摘要、改写、润色、生成结构化文本
- 不允许：虚构未由用户提供的关键事实；若素材不足，AI 必须显式说「根据现有素材」并提示补充

### 6.2 提示词工程
- 模板化 prompt，按「项目类型 + 模板 + 风格」三元组装
- 系统 prompt 中始终包含合规指令：「不得生成涉及未授权的隐私/歧视/医疗建议内容」

### 6.3 内容安全
- 输入侧：检测违规词、敏感人物、政治话题，命中则中断并提示
- 输出侧：AI 响应再过一道审核层；高风险项目（team/对外分享）默认开启

### 6.4 标识规范
- 数据层：每个段落记录 `provenance`：`human | ai_generated | ai_rewritten | mixed`
- 表现层：
  - 编辑器：AI 段落浅色底纹 + 「AI」徽标
  - 阅读页：段落右侧小标签 + 页面顶部横幅

### 6.5 模型矩阵（MVP）
| 能力 | 主选 | 备选 |
| --- | --- | --- |
| 通用对话 / 采访 / 叙事生成 | DeepSeek 或 通义千问 | GPT-4o（境外/合规接入） |
| 视觉理解（多模态） | GPT-4o（境内合规接入） | Qwen-VL |
| 向量检索 | bge-large（自部署或托管） + Milvus | — |
| 语音转写 ASR | 阿里云 ASR | Azure Speech |
| 语音合成 TTS | 阿里云 TTS | Azure Speech（阅读页朗读能力，二期） |
| 图像生成 | DALL·E 3 | Stable Diffusion（自部署，GPU 节点） |
| 视频生成 | 二期评估 | — |

---

## 7. 风险与依赖

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| LLM 服务波动 | 采访/成稿不可用 | 多模型路由（DeepSeek/Qwen/GPT-4o）+ 降级文案 + 重试 |
| 未授权素材传播 | 合规与品牌风险 | 上传即扫敏感信息 + 强授权 |
| 被采访者撤回后成稿残留 | 法律风险 | 撤回触发成稿脱敏/下线任务 |
| 用户对 AI 标识反感 | 体验争议 | 设置中提供「隐藏 AI 标识」选项但保留审计 |
| 跨境数据 | 监管风险 | 境内模型 + 境内存储 |
| 图像/视频生成合规 | 深度合成监管 | 启用阿里云/腾讯云合规审核通道 + 显著水印 |
| 知识图谱构建质量 | 时间线聚合、人脉推荐失真 | 人工审校闭环 + 置信度标记 |
| GPU 资源争抢 | 图像/视频任务排队 | 任务分级 + RabbitMQ 限流 + K8s 节点亲和 |

---

## 8. 度量与成功指标

### 8.1 北极星指标
**「完整完成一次 AI 采访并发布成稿的用户数 / 周」**

### 8.2 关键指标
| 指标 | 目标（MVP 结束） |
| --- | --- |
| 注册 → 完成首次采访转化率 | ≥ 35% |
| 采访完成 → 发布成稿转化率 | ≥ 50% |
| 平均成稿章节数 | ≥ 5 |
| 7 日留存 | ≥ 25% |
| 撤回授权率 | < 3% |
| 内容安全误拦率 | < 1% |

---

## 9. 后续演进（二期）

### 9.1 一期主动延后的能力
- **架构演进**：模块化单体 → 微服务拆分；Docker Compose → K8s；增加 Nacos 配置中心、独立 Spring Cloud Gateway
- **AI 能力扩展**：RAG + Milvus 向量库、知识图谱（人物-事件-时间-地点）、图像/视频生成、章节配图
- **多端**：微信小程序端（复用后端 API，前端重写）、移动端原生应用
- **产品形态**：年度回顾、专访稿、企业团队项目、公开阅读社区、付费订阅
- **协作深化**：多角色协同、审批流、批注采纳闭环
- **通知与可达性**：邮件、Web Push、PWA 高级优化、国际化（英文/繁体）
- **数字遗产**：被采访者去世后的归档与家属接管
- **可观测性**：SkyWalking 全链路追踪、Prometheus + Grafana 业务大盘

---

## 10. 开放问题（v1.1 已决策的标注 ✅）
1. 是否需要在 MVP 阶段提供「企业版」计费与多租户？—— **✅ 决策：二期**。
2. AI 标识是否允许在 personal 项目中默认隐藏？—— **✅ 决策：始终显示，Owner 无权关闭**。
3. 语音采访是否进 MVP？—— **✅ 决策：不做，文本优先**。
4. 团队项目的对外发布是否需要审批流？—— **✅ 决策：不做，team 项目整体延后**。
5. 移动端 H5 覆盖范围？—— **✅ 决策：关键页面（采访/查看/分享）用 Vant，其他页面响应式适配**。
6. 数据库中"交易"表的含义？—— **✅ 决策：单人 MVP 不实现付费，从 MySQL 范围移除**。
7. 知识图谱 MVP 范围？—— **✅ 决策：完全不做，二期启动**。
8. 4 个成稿模板？—— **✅ 决策：一期只做 2 个（人物小传 / 家族小传）**。
9. 部署形态？—— **✅ 决策：阿里云 ECS + Docker Compose，不上 K8s**。
10. LLM 失败降级？—— **✅ 决策：单模型 + 手工切换备用 Key，不做自动降级**。

---

## 11. 技术栈与约束

> 本节用于让产品/设计/法务了解落地形态；具体开发任务见《DevelopmentPlan.md》。

### 11.1 前端
- **桌面端**：Vue 3 + Vite + Element Plus
- **移动端 H5**：Vue 3 + Vite + Vant UI（断点切换）
- **PWA**：Service Worker 缓存采访草稿与最近阅读内容；Web Push 推送授权/成稿完成等通知
- **状态管理**：Pinia
- **路由**：Vue Router 4
- **HTTP**：Axios + 拦截器统一处理网关返回

### 11.2 后端（业务，🚩 单人版）
- **架构**：Spring Boot 3.x **模块化单体**（多 module，同进程）
- **模块划分**：
  - `module-account` 账号、工作区、项目
  - `module-auth` 认证、Token、权限
  - `module-memory` 人物/采访/素材/成稿/批注
  - `module-timeline` 时间线
  - `module-compliance` 授权、导出、删除、审计、分享
  - `module-bff` 前端聚合层
- **数据库**：
  - MySQL 8.0：账号、工作区、项目、人物、授权、分享等强事务
  - MongoDB 6.0：采访消息、成稿、批注、AI 结果
- **缓存**：Redis（缓存、会话、SSE 进度、限流）
- 🚩 **一期不引入**：Spring Cloud Gateway、Nacos、OpenFeign、RabbitMQ、Milvus

### 11.3 后端（AI，🚩 单人版）
- **框架**：FastAPI + Uvicorn + Pydantic v2
- **路由**：
  - `router/interview` AI 采访对话
  - `router/narrative` 叙事生成（**2 模板**：人物 / 家族）
  - `router/asset` 图像基础分析（OCR、敏感信息）
- **模型**：主选 **DeepSeek**，备选 **Qwen**（手工切换）
- **异步**：FastAPI BackgroundTasks + Redis Streams（不引入 Celery/RabbitMQ）
- 🚩 **一期不引入**：multimodal、rag、media-gen、ASR/TTS 路由

### 11.4 存储与分发
- **对象存储**：阿里云 OSS
- **CDN**：阿里云 CDN
- **上传方式**：服务端 STS 签名直传 + Referer 防盗链

### 11.5 可观测性（🚩 大幅简化）
- 不引入 SkyWalking / Prometheus / Grafana / ELK
- 使用 **logback JSON + Python logging JSON** 输出到文件
- 通过 `grep` / `less` / 简单脚本排查

### 11.6 部署（🚩 大幅简化）
- **阿里云 ECS 单机** + **Docker Compose**（**仅生产环境**使用）
- Nginx 处理 HTTPS、域名、静态资源
- 🚩 **一期不引入**：K8s、Harbor、ArgoCD、独立 Gateway 节点
- 环境：dev（本地原生服务）/ staging（ECS 测试）/ prod（ECS 正式）

### 11.7 关键技术约束
- 前后端通过 `module-bff` 统一对外，前端不再直连业务模块
- AI 任务一律走 HTTP 异步 + SSE 进度回写 Redis
- 重要数据走 MySQL；非结构化/AI 结果走 MongoDB
- 任何对外分享链接的 token 独立生成，不暴露资源 ID

### 11.8 本地开发环境（v1.1 增补）
> 单人本地开发环境已具备，**无需额外安装 Docker**。

| 组件 | 版本 | 用途 |
| --- | --- | --- |
| JDK | 17 LTS（与 JDK 8 共存，IDE 切换） | Spring Boot 3.x 编译与运行 |
| Maven | 3.9.9 | 后端构建 |
| Node.js | 20 LTS | 前端 Vite 构建 |
| Python | 3.11 | FastAPI / AI 服务 |
| MySQL | 8.0（admin/1234，**仅本地**） | 业务强事务数据 |
| MongoDB | 6.0 | 文档/AI 结果 |
| Redis | 6+ | 缓存/会话/SSE 进度 |
| IntelliJ IDEA / VS Code | — | 开发工具 |

- **数据库库名**：`moment_weaver`（MySQL 与 MongoDB 同名）
- **MySQL 凭据仅本地使用**，通过 `application.yml` 的 `${MYSQL_PASSWORD:}` 占位从 `.env` 注入，**严禁**入 git
- **服务启动顺序**：MySQL / MongoDB / Redis（系统服务）→ Spring Boot → FastAPI → Vue Vite

---

## 12. MVP 切割与单人开发说明（v1.1 增补）

> 本节为单人 + Vibe Coding 模式下的范围再确认，方便后续执行时对齐。

### 12.1 切割原则
- **端到端跑通最瘦切片** 优先于「功能齐全但跑不通」
- **合规底线**（授权、删除、AI 标识、数据最小化）一刀不砍
- **好看/好运营**（高级 PWA、Web Push、邮件）一律二期
- **架构上复杂的**（微服务、GPU、知识图谱、RAG）一律二期
- **可运营性**（完善监控、多模型降级）一期手工兜底

### 12.2 一期硬指标
- 「注册 → 建项目 → 添加人物 → 授权 → 采访 → 生成小传 → 分享」主链路 30 分钟跑通
- LLM 失败时**手动切 Key**，不要求自动降级
- 单 ECS（4C8G 起）能扛 5-10 并发采访会话

### 12.3 一期明确不做
- 微服务、K8s、GPU
- 知识图谱、RAG、Milvus
- 图像/视频生成
- 多模型自动降级
- 视频采访、实时多人
- 邮件通知、Web Push
- i18n（仅 zh-CN）
- 团队项目类型、多角色协同
- 高级 PWA
- 完善的可观测性（仅文件日志）

### 12.4 二期优先序（按 ROI）
1. 微服务拆分（团队扩到 3 人时启动）
2. RAG + Milvus（提升成稿质量）
3. 微信小程序端
4. 知识图谱
5. 邮件 / Web Push
6. 团队项目 + 企业版
7. 图像/视频生成（GPU）